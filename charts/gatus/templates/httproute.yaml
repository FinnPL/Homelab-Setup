apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatus
  namespace: gatus
spec:
  parentRefs:
    - name: main-gateway
      namespace: gateway
  hostnames:
    - status.lippok.dev
  rules:
    - backendRefs:
        - name: gatus
          port: 80
